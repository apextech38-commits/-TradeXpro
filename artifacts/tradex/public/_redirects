# Static assets — serve directly, never rewrite to index.html
/assets/*   /assets/:splat  200

# SPA fallback — all other paths serve index.html
# (hash routing handles the rest client-side)
/*          /index.html     200
