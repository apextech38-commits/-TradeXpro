name = "tradexpro"
pages_build_output_dir = "artifacts/tradex/dist/public"
compatibility_date = "2024-01-01"
# cache bust 20260626T183920Z

# NOTE: this file previously had a second, invalid duplicate `name = 'tradexpro-backend'`
# key appended below the real config -- TOML doesn't allow duplicate keys at the same
# table level, so that line was either silently ignored or causing parse issues. The
# `tradexpro-backend` Cloudflare Workers project (if still needed) should get its own
# separate wrangler.toml in whatever directory contains its actual source, not share
# this one, which belongs to the `tradexpro` Pages project.

# D1 binding for Smart Copy (functions/api/copy-trading/*). Database and schema
# already provisioned; see migrations/0001_copy_trading.sql for the schema itself.
[[d1_databases]]
binding = "DB"
database_name = "tradexpro-copytrading"
database_id = "b015d7df-fa59-4efe-aee7-6812f31a5319"
